# Frontend environment for the Copilot app
# Copy to `copilot-app/.env.local` and adjust as needed.

# WebSocket URL of the FastAPI backend (Phase 2). Default if omitted:
#   ws://127.0.0.1:8000
NEXT_PUBLIC_WS_URL=ws://127.0.0.1:8000
